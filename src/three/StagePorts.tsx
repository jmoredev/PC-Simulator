import { BoardBase } from './ComponentModel'

/**
 * Fase 2: la misma placa que se montó en la fase 1, vista de frente por su
 * parte trasera. Los cables llegan desde la barra lateral.
 */
export function StagePorts() {
  return <BoardBase />
}
